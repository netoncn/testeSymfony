{% extends 'base.html.twig' %}

{% block title %}Ferramentas{% endblock %}

{% block body %}
    <h1>Ferramentas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ferramenta.id }}</td>
            </tr>
            <tr>
                <th>Cod_ferramenta</th>
                <td>{{ ferramenta.codFerramenta }}</td>
            </tr>
            <tr>
                <th>Nome_ferramenta</th>
                <td>{{ ferramenta.nomeFerramenta }}</td>
            </tr>
            <tr>
                <th>Marca_ferramenta</th>
                <td>{{ ferramenta.marcaFerramenta }}</td>
            </tr>
            <tr>
                <th>Aluguel_hora</th>
                <td>{{ ferramenta.aluguelHora }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ferramentas_index') }}">back to list</a>

    <a href="{{ path('ferramentas_edit', {'id': ferramenta.id}) }}">edit</a>

    {{ include('ferramentas/_delete_form.html.twig') }}
{% endblock %}
